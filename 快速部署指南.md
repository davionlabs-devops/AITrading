# APEX AI 交易策略工具 - 快速部署指南

> ⚡ 5 分钟快速部署，适合有 Docker 经验的用户

## 🚀 超快速部署（3 步）

### 前提条件
- ✅ 已安装 Docker Desktop（Windows/macOS）或 Docker（Linux）
- ✅ 已下载项目文件

### 步骤 1：准备配置

```bash
# 进入项目目录
cd /Users/ez/Downloads/ez1/nofx-dev

# 复制配置文件
cp config.json.example config.json
```

### 步骤 2：启动服务

```bash
# 给脚本添加执行权限
chmod +x start.sh

# 启动（第一次会自动构建，需要 5-10 分钟）
./start.sh start --build
```

### 步骤 3：访问界面

打开浏览器访问：**http://localhost:3000**

**完成！** 🎉

---

## 📋 常用命令

```bash
# 启动服务
./start.sh start --build

# 停止服务
./start.sh stop

# 重启服务
./start.sh restart

# 查看日志
./start.sh logs

# 查看状态
./start.sh status
```

---

## ⚠️ 遇到问题？

1. **Docker 未安装**：访问 https://www.docker.com/products/docker-desktop/ 下载安装
2. **端口被占用**：修改 `config.json` 中的 `api_server_port`
3. **查看详细文档**：`docs/详细部署指南.md`

---

**需要更详细的说明？请查看 `docs/详细部署指南.md`**

